---
title: Enforcing Dirichlet Boundary Conditions in Operator Learning
title_zh: 在算子学习中强制执行Dirichlet边界条件
authors: "Andrew M. Stuart, Margaret Trautner"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27256v1"
tags: ["query:ai-physics"]
score: 9.0
evidence: 面向PDE的算子学习，显式强制执行狄利克雷边界条件，属于物理科学机器学习
tldr: 在科学机器学习中，算子学习旨在逼近无穷维函数空间之间的映射，常源于PDE解算子，但多数神经算子通过训练数据间接学习边界条件。本文提出一种新架构，通过对每层输出施加约束，使其落入输出域上Laplacian齐次Dirichlet特征函数的张成子空间，从而独立于训练严格满足边界条件。该方法仅要求输出域有界且具有Lipschitz边界，不限制网格类型，适用于任意网格与几何。作者证明了该架构的普适逼近性，并统合了多类核积分神经算子的理论。在二维Darcy流（方形域）与Helmholtz方程（圆形域）上的实验验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.27256v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1009, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1008, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1018, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.27256v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1020, \"height\": 671, \"label\": \"Figure\"}]"
motivation: 现有神经算子大多从数据中间接学习边界条件，缺乏显式满足已知边界条件的机制，且已有显式方法限制苛刻。
method: 将每层输出限制在输出域Laplacian齐次Dirichlet特征函数的张成空间，从而保证边界条件，支持任意网格和Lipschitz域。
result: 在Darcy流和Helmholtz方程上验证了有效性，并证明普适逼近性，同时统一了核积分神经算子的相关理论。
conclusion: 为算子学习提供一种通用且可严格满足边界条件的架构，兼顾表达力与理论保证。
---

## 摘要
科学机器学习中的算子学习关注无限维函数空间之间映射的逼近；这类映射经常作为偏微分方程（PDE）的解算子出现。神经算子在从数据逼近此类映射方面已展现出广泛的实证成功。然而，大多数现有的神经算子架构通过从数据训练间接地强制执行边界条件，尽管边界条件通常已知精确。此外，现有的显式强制边界条件的修改和方法受到不切实际的限制，包括边界光滑性、均匀网格以及可分离的盒状域。在这项工作中，我们提出了一种架构，该架构独立于训练，满足齐次Dirichlet边界条件，同时保留现有核积分神经算子架构的表达能力。这是通过强制每层输出包含在输出域上Laplacian的齐次Dirichlet特征函数子集的张成中来实现的。该方法仅要求输出域具有Lipschitz边界且有界，对离散化的选择没有限制，使其适用于任意网格数据和一般几何形状。我们证明了所提架构的普适逼近性；此外，我们在分析中采用的方法证明了广泛类别的核积分神经算子的普适性，从而统一了各种算子学习方法现有的理论。我们在二维PDE中由系数到解映射定义的映射上验证了所提方法：方形域上的Darcy流和圆形域上的Helmholtz方程。并与替代方法进行了比较。

## Abstract
Operator learning in scientific machine learning is concerned with approximation of maps between infinite-dimensional function spaces; such maps frequently arise as the solution operators of partial differential equations (PDEs). Neural operators have demonstrated broad empirical success at approximating such maps from data. However, most existing neural operator architectures enforce boundary conditions indirectly through training from data even though the boundary condition is often known exactly. Furthermore, existing modifications and approaches that do enforce boundary conditions explicitly suffer from impractical restrictions, including boundary smoothness, uniform grids, and separable, box-like domains. In this work, we propose an architecture which, independently of training, satisfies homogeneous Dirichlet boundary conditions, whilst simultaneously retaining the expressivity of existing kernel-integral neural operator architectures. This is achieved by enforcing the property that the output of each layer is contained in the span of a subset of the homogeneous Dirichlet eigenfunctions of the Laplacian on the output domain. The method requires only that the output domain be bounded with Lipschitz boundary and places no restriction on the choice of discretization, making it applicable to arbitrary mesh data and general geometries. We prove universal approximation for the resulting architecture; furthermore the approach we adopt in the analysis proves universality for a broad class of kernel-integral neural operators thereby uniting existing theory for a variety of operator learning methods. We validate the proposed method on maps defined by the coefficient to solution map in 2D PDEs: Darcy flow on a square domain and the Helmholtz equation on a circular domain. Comparisons are made with alternative methods.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **问题背景**：算子学习旨在用神经网络逼近无限维函数空间之间的映射，这类映射常由 PDE 解算子定义（如系数到解映射）。主流神经算子架构（如 DeepONet、FNO、PCA-Net 等）虽然经验上成功，但通常**通过训练数据间接学习边界条件**，即使边界条件是精确已知的，也无法在数学上严格满足。
- **现存显式方法的局限**：已有显式强制边界条件的方法（如 OPNO、SPFNO）依赖 Chebyshev-Gauss-Lobatto 点或均匀网格，且局限于可分离的盒状域，难以推广到任意复杂几何与任意网格数据。
- **论文核心目标**：提出一种**独立于训练、自动满足齐次 Dirichlet 边界条件**的神经算子架构，同时保留核积分神经算子的表达能力，适用于**任意有界 Lipschitz 域**与**任意离散化**。此外，作者希望其理论框架能**统一现有多种核积分神经算子的普适逼近证明**。

## 2. 方法论

- **核心思想**：利用输出域上 Laplacian 的**齐次 Dirichlet 特征函数**作为每层的基函数，将每层输出限制在这些特征函数张成的子空间中，从而在构造上保证边界处取值为零。
- **关键技术细节**：
  - 采用非局部神经算子（Nonlocal Neural Operator, NNO）框架，其隐藏层定义为：
    \[
    (L_\ell v)(x)=\sigma\left(W_\ell v(x)+b_\ell+\sum_{m=1}^M \langle T_{\ell,m} v, \psi_{\ell,m}\rangle_{L^2(\Omega)}\phi_{\ell,m}(x)\right)
    \]
  - 提出的 **Dirichlet 层（Dirichlet Layer）** 将基函数直接设为 \(\psi_{\ell,m}=\phi_{\ell,m}=\eta_m\)，其中 \(\{\eta_m\}\) 是输出域上 Laplacian 的 Dirichlet 特征函数。
  - 将 Dirichlet 层作为**最终隐藏层**并设置 \(W_\ell=0\)、\(b_\ell=0\)，同时使投影层 Q 保持零（使用零保持激活函数，如 GeLU），即可保证输出在边界上严格为零。
  - 非零常数 Dirichlet 边界条件可通过预处理/后处理（学习 \(u-b\mathbf{1}\) 再加回）处理。
- **理论结果**：
  - 提出 **Smoothed Approximation Assumption（光滑逼近假设）**，涵盖 Fourier 基、Laplacian 特征函数基、Legendre 多项式基等常见选择。
  - **Theorem 3.4（单基元素）**：在输出基函数足够光滑（\(C^{s'}(\bar\Omega)\)）时，仅用**单个隐藏层、单对基函数**即可实现 \(C^{s'}\) 意义下的普适逼近。
  - **Theorem 3.5（使用 NNO 自身基元素）**：在更一般的条件下，利用架构自身的基函数进行逼近，适用于 Laplacian 特征函数（它们在一般 Lipschitz 域上仅在 \(\Omega\) 内部光滑）。
  - **Corollary 3.6**：Dirichlet 层最终层配置下，对任意连续算子 \(\Psi^\dagger: C^s(\Omega)\to C^0(\Omega)\) 保持普适逼近性，且在迹意义下输出边界值恒为零。
  - 关键证明工具包括：将真实算子分解为连续泛函加权基函数之和（Lemma 3.8）、用 NNO 近似连续泛函与基函数乘积的 Lemma 3.10/3.11 等。

## 3. 实验设计

- **场景 1：Darcy 流（方形域）**
  - 方程：\(-\nabla\cdot(a\nabla u)=1\)，\(\Omega=[0,1]^2\)，\(u|_{\partial\Omega}=0\)。
  - 数据集：采用 FNO 原论文 [28] 的数据集，共 2048 个样本（248 验证、200 测试），求解器为 421×421 二阶有限差分。
  - 网络：前 4 层为 FNO 层，最后 1 层替换为 DNO 层；12 modes/维、channel width 64、batch size 15、300 epochs。
- **场景 2：Helmholtz 方程（圆域）**
  - 方程：\(-\Delta u-\omega^2 a u=0\)，\(\omega=5\pi/2\)，\(\Omega\) 为单位圆盘，\(u|_{\partial\Omega}=b\)（非零常数边界）。
  - 数据集：自生成 11000 个样本（500 验证、500 测试），系数 \(a\) 为高斯和并归一化到 \([0,1]\)，\(b\sim U[0.25,5]\)；使用 FEniCSx 三角形网格 CG1 有限元求解；通过条件化剔除接近奇异点的样本（\(\max|u|/b>10\)）。
  - 网络：所有隐藏层均采用 DNO 层（16 modes、width 64、batch size 25、300 epochs、4 层）。
- **Benchmark/对比方法**：
  - 标准 FNO（在均匀网格上训练）；
  - FNO 训练后将输出投影到前 J 个 Laplacian 特征函数（"projected after training"）；
  - 训练过程中加入该投影（"projected during training"）；
  - 纯线性基线：对特征函数系数做岭回归（ridge regression）；
  - 论文所提方法：Dirichlet Neural Operator（DNO）。

## 4. 资源与算力

- 论文**未明确说明**所用的 GPU 型号、数量等硬件资源。
- 仅在图 3 与图 4 的超参数搜索可视化中报告了训练时间：Darcy 场景各组训练时间约 **50–200 分钟**，Helmholtz 场景约 **5–40 分钟**，且说明 DNO 与 FNO 训练时间大致相当。
- 由于硬件细节缺失，无法精确复现算力需求；但可推断计算规模对常规单卡 GPU 而言是可负担的。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个不同 PDE 场景（Darcy 流方形域、Helmholtz 圆域），涵盖均匀网格与非结构三角形网格、零与非零 Dirichlet 边界。
  - 超参数网格搜索（modes、channel width、batch size、layers、epochs）。
  - 数据效率实验（N 从约 10 到 1600/10000），每条数据量重复训练 5 个模型并报告均值 ± 2 标准差。
  - 最优/中位/最差测试样本的可视化分析。
- **充分性评估**：
  - 优点：数据效率曲线对比了 5 种方法，覆盖低数据与高数据 regime，并提供了不确定性区间；Helmholtz 实验特意展示了对非均匀网格和非零边界的适用性；超参数搜索结果显示 DNO 对次优超参数更鲁棒。
  - 不足：未与同样显式满足边界条件的已有方法（如 OPNO/SPFNO）直接对比；没有消融实验探讨不同基函数数量、不同激活函数、不同域形状（如 3D 域、带角点/凹域的复杂几何）的影响；仅报告相对测试误差，未给出计算 FLOPs 或推理成本对比；论文声称 DNO 与 FNO 训练时间相当，但未给出系统的运行时间表格。

## 6. 主要结论与发现

- **边界条件绝对满足**：DNO 架构在训练前即可保证齐次 Dirichlet 边界条件，且对任意网格与一般有界 Lipschitz 域适用，突破了以往方法对均匀/高斯网格与可分离盒状域的限制。
- **普适逼近理论**：定理 3.4 与 3.5 为核积分神经算子提供了统一的普适逼近证明框架，同时覆盖 FNO、ANO、Legendre/Chebyshev 类谱算子以及 Laplacian 特征函数基等类别，具有独立理论价值。
- **数值效果（Darcy）**：低数据 regime（N≈10–80）下 DNO 精度约为其他非线性方法（FNO、投影 FNO）的 2 倍，甚至优于岭回归不明显；1600 数据点时仍比 FNO 高约 20% 精度。投影 FNO（无论训练前/后）并未显著改善性能，说明 DNO 的优势不仅在于边界条件本身。
- **数值效果（Helmholtz）**：低数据 regime 各方法相近，但高数据 regime（N=5000 与 10000）DNO 精度约为 FNO 的 2 倍；训练中投影的 FNO 在高数据 regime 略有改善，而训练后投影导致误差翻倍以上（归因于 FEM 网格与均匀网格之间的插值误差）。
- **验证非线性必要性**：岭回归在所有数据量下表现很差，说明所学的系数-解映射确实是高度非线性的。

## 7. 优点

- **通用性与可部署性**：仅要求输出域有界且 Lipschitz 边界，不限制网格类型与维度，可处理任意网格数据与一般几何；将最终隐藏层替换为 Dirichlet 层即可将任何核积分算子（如 FNO）改造为严格满足边界条件的版本，工程改动极小。
- **理论贡献扎实**：
  - 弱化了以往普适逼近证明对输出空间正则性的要求，将 Smoothed Approximation Assumption 与具体基函数选取解耦；
  - Lemma 3.11 利用架构自身基函数完成逼近，比单基函数定理更贴合实践；
  - 统一了 FNO、ANO、谱方法等多类核积分神经算子的逼近理论。
- **理论—实践契合**：Corollary 3.6 在仅假设输出连续（\(C(\Omega)\)）的情况下证明普适性，与 Laplacian 特征函数在一般 Lipschitz 域上的实际正则性相匹配。
- **计算复杂度可控**：一层 Dirichlet 层的复杂度为 O(NM)，经验上 \(M\approx \log N\) 即足够，与 FNO 的 O(N log N) 在实践中相当。
- **实验设计细致**：数据效率曲线与重复训练误差区间、超参数网格搜索对比、极端最优/中位/最差样本可视化，增强了结论的可靠性。

## 8. 不足与局限

- **局限性（理论层面）**：
  - Corollary 3.

## 8. 不足与局限（续）

- **局限性（理论层面）**：
  - Corollary 3.6 虽然在 \(C^s(\Omega)\to C^0(\Omega)\) 意义下证明了普适性，但实际 PDE 解算子常常要求输出具有更高正则性（如 \(C^{s'}(\bar\Omega)\)），且单基元素定理（Theorem 3.4）对基函数光滑性的要求较严格，在一般 Lipschitz 域上 Laplacian 特征函数仅内部光滑，边界附近的正则性受限于域几何，因此理论与实际逼近精度之间仍存在间隙。
  - 非零 Dirichlet 边界的处理采用“学习 \(u-b\mathbf{1}\) 再加回”的预处理/后处理策略，但该方法的误差传播与分析未在理论上严格刻画；对非恒定边值函数的情形也未给出误差界。
  - 定理主要针对齐次 Dirichlet 边界，对 Neumann、Robin 或混合边界条件的推广在文中仅作为讨论提及，并未给出对应的特征函数基构造与证明。
- **局限性（实验层面）**：
  - 实验仅涉及二维域（方形与圆盘），未验证三维域、带角点/凹角/裂缝等复杂 Lipschitz 域，也未展示在完全非结构网格（如任意多边形网格）上的表现。
  - Helmholtz 实验虽使用了非均匀 FEM 网格，但最终误差报告基于均匀网格测试点，插值误差的影响未与 DNO 方法本身解耦。
  - 未对 Dirichlet 层的基函数数量 \(M\)、激活函数选择、层数等关键因素做系统消融，难以判断各设计选择的独立贡献。
  - 与 OPNO、SPFNO 等显式边界方法的数值对比缺失，削弱了对“DNO 优于现有显式方法”这一主张的论证力度。
  - 计算效率对比仅以“训练时间大致相当”一笔带过，未给出参数量、推理时间、内存占用等具体数据。

## 9. 参考文献质量与相关性

- **总体评价**：参考文献覆盖面良好，既包含算子学习领域的经典工作（DeepONet、FNO、PCA-Net、LOCA，等），也包含函数空间逼近理论、谱方法与积分方程相关文献，还涉及 PDE 数值求解（有限元、谱方法）等支撑性工作。
- **相关性**：绝大多数引文直接支撑论文的动机（如现有神经算子边界条件隐式学习问题）、方法设计（非局部神经算子框架、Laplacian 特征函数性质）与理论证明（连续泛函逼近、核积分表示）。对 FNO 原论文 [28] 的引用直接对应实验基准。
- **时效性**：引文中既包含 20 世纪 90 年代至 2010 年代的理论经典（如泛函分析、特征函数展开、谱方法），也包含近年的深度算子学习文献，整体时效性合理。但缺少对 2023 年后最新相关工作的引用，可能因研究周期所致。
- **规范性**：引用格式统一，arXiv 预印本与期刊/会议论文均有标注，未发现明显遗漏或无关引用。

## 10. 主要贡献与最终评价

- **主要贡献**：
  1. 提出 **Dirichlet Neural Operator（DNO）**，一种在构造上严格满足齐次 Dirichlet 边界条件的神经算子架构，适用于任意有界 Lipschitz 域与任意离散化，并可通过对最终隐藏层的简单替换集成到现有核积分神经算子中。
  2. 建立**统一的普适逼近理论框架**，将此前分散的 FNO、ANO、Legendre/Chebyshev 谱算子等逼近结果纳入同一证明体系，并弱化了对输出空间正则性的假设。
  3. 通过 Darcy 流与 Helmholtz 方程上的系统性实验，验证了 DNO 在低数据 regime 和高数据 regime 下均优于标准 FNO 及两种 FNO 投影变体，并展现出对非均匀网格与非零边界条件的稳健性。

- **最终评价**：该论文在“如何让神经算子严格满足边界条件”这一重要且实际的问题上，提出了一个简洁、通用且理论完整的解决方案。其方法具有显著的工程可落地性——只需将最终隐藏层替换为 Dirichlet 层即可获得严格的边界保证。理论部分统一并推进了神经算子逼近理论，具有独立的学术价值。实验设计虽覆盖两个典型场景并提供了数据效率分析，但在复杂几何扩展、与既有显式边界方法对比、消融分析等方面仍有提升空间。总体而言，这是一篇方法、理论、实验三者结合良好、实用价值明确的高质量论文，对算子学习领域关于边界条件处理的讨论有重要推动作用。

（完）
