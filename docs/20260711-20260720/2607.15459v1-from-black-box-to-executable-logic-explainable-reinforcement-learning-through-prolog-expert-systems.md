---
title: "From Black Box to Executable Logic: Explainable Reinforcement Learning through Prolog Expert Systems"
title_zh: 从黑盒到可执行逻辑：通过Prolog专家系统实现可解释强化学习
authors: Eduardo C. Garrido-Merchán
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15459v1"
tags: ["query:xrl-symbolic"]
score: 9.0
evidence: 将强化学习策略提取为Prolog规则，类似于符号回归
tldr: 深度强化学习策略是黑箱，难以理解和验证。本文提出将PPO教师策略转化为可读、可执行、可编辑的Prolog逻辑程序，通过三阶段后处理提取规则列表并逐步优化。理论证明转化保证返回损失界，连续观测下近似转换的代价与分辨率反比。实验表明，离散任务获得最优回报，连续任务Acrobot和CartPole接近教师性能，LunarLander部分恢复与下界一致。该方法提供了可验证的可解释性，为安全关键应用奠定基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15459v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1575, \"height\": 231, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 344, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1520, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1519, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 977, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1722, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15459v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1449, \"height\": 484, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15459v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1580, \"height\": 209, \"label\": \"Table\"}]"
motivation: 训练好的深度强化学习策略是黑箱，缺乏可解释性和可验证性，需要转化为可读且可执行的逻辑程序以便理解、运行和优化。
method: 三阶段后处理变换：提取冻结PPO教师、归纳有序规则列表、输出Prolog程序；后续通过策略评估编辑规则并保证单调提升，理论证明收敛性。
result: "离散任务（16894状态）获得最优回报；连续任务Acrobot匹配教师、CartPole恢复97%回报，LunarLander部分恢复，与指数下界预测一致。"
conclusion: 将黑箱策略转化为可执行逻辑程序，提供可验证的返回损失界和单调改进保证，实现了可解释性与性能的平衡。
---

## 摘要
训练好的深度强化学习策略是一个黑盒，我们探究能否通过将其重写为可执行逻辑程序来使其可解释，该程序能够复现其行为，且人可以阅读、逻辑引擎可以运行、优化器可以编辑。我们提出了一种三阶段事后转换方法：首先提取一个冻结的近端策略优化教师模型，然后以经典关系学习的方式从其决策中归纳出有序规则列表，最后将结果输出为一个Prolog程序，其每个决策均由现成的逻辑引擎执行；后续的扩展阶段会编辑规则库，并且仅在策略评估确认回报提升时才接受编辑。我们证明了四个保证。回报损失上界使得蒸馏程序成为有限马尔可夫决策过程中的机器可检查证书，并且扩展循环单调改进并终止。对于连续观测设置，我们回答了转换是否可能：命题阈值实例化将网络以任意保真度转换，随着分辨率B增大，不一致性为O(1/B)，回报差距以相同速率闭合，并且一个匹配的下界表明，对于倾斜决策边界，代价在观测维度上呈指数增长。实验上，在一个具有16,944个可达状态的双房间钥匙-门任务中，扩展后的Prolog程序在每个种子中均达到精确最优回报，并且在预算限制条件下，在十个种子中的精确回报上均超过了随机教师模型。在三个连续控制任务中，输出的程序替代了网络：在Acrobot上以11条子句匹配了含噪声的神经教师模型，在CartPole上恢复了约97%的回报，而在更精细控制的LunarLander上仅部分恢复，恰好达到了指数下界预测的上限。

## Abstract
A trained deep reinforcement learning policy is a black box, and we ask whether it can be made explainable by rewriting it as an executable logic program that reproduces its behaviour and that a person can read, a logic engine can run, and an optimizer can edit. We present a three-stage post-hoc transformation that extracts a frozen proximal policy optimization teacher, induces an ordered rule list from its decisions in the manner of classical relational learning, and emits the result as a Prolog program whose every decision is executed by an off-the-shelf logic engine; a subsequent expansion stage edits the rule base and accepts an edit only when policy evaluation certifies a return increase. We prove four guarantees. A return-loss bound makes the distilled program a machine-checkable certificate in a finite Markov decision process, and the expansion loop improves monotonically and terminates. For the continuous-observation setting we answer whether the conversion is possible at all: the propositional threshold instantiation converts the network to arbitrary fidelity as the resolution B grows, with disagreement O(1/B) and a return gap that closes at the same rate, and a matching lower bound shows the cost is exponential in the observation dimension for an oblique decision boundary. Empirically, on a two-room key-and-door task with 16,944 reachable states the expanded Prolog program attains exact optimal return in every seed and, in a budget-capped regime, exceeds the stochastic teacher on exact return in ten of ten seeds. On three continuous-control tasks the emitted program substitutes the network, matching the neural teacher within noise on Acrobot with eleven clauses and recovering about 97% of its return on CartPole, while on the finer-control LunarLander it recovers only partially, exactly the ceiling the exponential lower bound predicts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

深度强化学习（DRL）策略是密集数值参数构成的“黑箱”，人类难以直接理解其行为、验证其可靠性或进行编辑。虽然已有方法如显著性图、代理评分或反事实探测提供间接解释，但都不产生人可读、逻辑引擎可执行、可编辑的产物。  
本文提出将冻结的深度强化学习策略（PPO）转化为**可执行的Prolog逻辑程序**，该程序能复制教师策略的行为，并且具有三个关键特性：事后提取（不修改教师训练）、可执行（直接由SWI-Prolog运行）、可超越（通过回报驱动的扩展阶段改进教师）。

### 2. 论文提出的方法论

- **核心思想**：通过三阶段后处理流水线，将神经策略转化为有序决策规则列表，再通过精确策略评估进行单调提升。  
- **关键技术细节**：
  1. **提取阶段**：在模型访问条件下枚举所有可达状态（有限MDP），查询冻结教师的贪心动作（argmax）。保留教师回滚下的折扣访问分布。
  2. **归纳阶段**：采用类似FOIL算法的贪心集合覆盖策略，从带权重的状态标签中生长有序一阶子句列表。子句体为谓词逻辑文字（如方向、状态属性），由回溯搜索（SLD解析）确定第一个满足的基例。默认子句保证确定性策略。最终输出Prolog程序，包含感知层、路径搜索（有向图BFS）以及决策列表（带cut确保第一匹配）。
  3. **扩展阶段**：在精确回报oracle（通过求解Bellman线性系统得到）上执行爬山搜索。编辑操作包括添加子句、重排子句、剪枝（除默认子句外）。仅接受至少提高阈值的编辑。该循环单调提升并有限终止。
- **理论保证**：
  - **定理1（回报损失界）**：蒸馏学生策略与贪心教师之间的回报差距 ≤ 2R_max ϵ†/(1-γ)²，其中ϵ†是按贪心教师折扣访问加权的分歧率。在有限MDP中可精确计算，成为机器可检查证书。
  - **命题1（扩展循环单调性与终止）**：编辑次数 ≤ 2R_max/((1-γ)τ)，最后输出τ-局部最优。
  - **定理2 & 推论2（连续观测下的近似转换）**：对连续观测空间，使用基于分位数的B-bin网格将特征离散化后，最佳可达到的分歧率ϵ†_B → 0，且ϵ†_B ≤ C ρ_max P / B，回报差距以1/B速率闭合。
  - **命题3（诅咒维度下界）**：对于倾斜决策边界（非轴对齐），任何轴对齐规则列表需要Ω(B^{d-1})个单元覆盖边界，且ϵ† ≥ c/B。这解释了LunarLander上的精度上限。

### 3. 实验设计

- **数据集/场景**：
  - **KeyDoor**：自定义离散确定性网格世界，两房间四行布局，需捡钥匙、开门、到目标。共16,944个可达状态。使用两种教师训练机制：R1（收敛，贪心策略精确成功率≥0.95后停止）和R2（预算限制29.9万步，贪心策略明显未收敛）。
  - **连续控制任务**：基于Gymnasium的LunarLander（8维观测，4动作）、CartPole（4维，2动作）、Acrobot（6维，3动作）。
- **基准对比**：
  - KeyDoor：对比随机教师、贪心教师、蒸馏学生、扩展学生。
  - 连续任务：对比naive学生（仅使用教师访问状态蒸馏）、DAgger学生（迭代收集学生轨迹并由教师重标记）、教师本身。分辨率B扫过{2,3,4,6,8,12}。
- **评估指标**：精确回报（KeyDoor通过线性系统求解）、蒙特卡洛回报（连续任务，同一种子流配对）、精确成功率、分歧率、子句数量。
- **统计方法**：四分位均值（IQM）加分层bootstrap置信区间（Agarwal et al. 2021），Wilcoxon符号秩检验（Bonferroni校正）。

### 4. 资源与算力

论文**未明确说明使用的GPU型号、数量或训练时长**。仅提到PPO教师使用8个同步环境训练，KeyDoor收敛时每个种子在369k-451k步内停止，预算限制为299,008步。连续任务未报告具体GPU资源。没有运行时长或FLOPs信息。因此无法评估算力开销。

### 5. 实验数量与充分性

- **KeyDoor**：两个训练机制×10个种子×（教师、贪心、蒸馏、扩展）共40个策略。每个种子所有精确量通过线性系统计算，无采样噪声。扩展轨迹每个种子记录每一步编辑。
- **连续任务**：三个任务×5个种子×6个分辨率×（naive、DAgger、教师）≈ 90组×每个策略被评估（蒙特卡洛100个episodes？未具体说明）。统计采用IQM和bootstrap，并报告配对差异。
- **充分性评价**：
  - 离散实验非常充分：精确计算、20个种子（10+10）、两个教师机制，展示扩展超越教师的普遍性。但证书空洞问题被如实指出。
  - 连续实验覆盖三个难度层次，分辨率扫过6个值，但种子数仅5个，Wilcoxon检验p值下限为0.0625，论文明确不依赖显著性而基于效应量和置信区间。无大型消融（如不同归纳算法、不同编辑算子、不同教师架构）。无真实机器人实验或复杂3D环境。
  - 总体客观，但连续实验的统计功效偏低，LunarLander上DAgger未达到教师可能由下界解释，但未实验更高分辨率（如B=24）验证趋势。

### 6. 论文的主要结论与发现

- KeyDoor中，扩展Prolog程序在两个机制下均达到**精确最优回报**（0.812204，成功概率1.0）。R2中扩展学生10/10种子超过随机教师，平均回报提升0.1001。
- 连续任务中，DAgger方法使符号学生表现：CartPole恢复约97%教师回报（≈484 vs 496），Acrobot在B=8时匹配教师（-83.5 vs -83.8），LunarLander仅部分恢复（+155 vs 221），与指数下界预测一致。
- 理论保证得到实验验证：离散任务中所有学生/教师对满足回报损失界（虽然空洞）；连续任务中分歧率随B单调下降，子句数随B增长。
- 符号学生可读、可执行、可超越教师，且扩展阶段永不退化。

### 7. 优点

- **理论贡献扎实**：给出回报损失界、单调改进终止保证、连续观测下的收敛速率和维度诅咒下界，证明可转化为任意精度但需指数级规则。
- **流程完整**：从提取、归纳到扩展，生成真正可运行的程序（SWI-Prolog），而非仅决策树或命题规则。
- **可超越教师**：通过回报驱动扩展而非仅模仿，可纠正教师错误，这在KeyDoor中得到证实。
- **透明实验**：离散任务使用精确计算，无采样噪声；连续任务报告配对比较、置信区间，明确说明证书空洞和统计不显著情况。
- **可读性**：产出如Listing 1和2的紧凑规则列表（如Acrobot 11条子句），人类可直接理解。

### 8. 不足与局限

- **规模限制**：精确评估需要模型访问和状态枚举，KeyDoor仅1.7万状态。连续任务中DAgger代替枚举，但精确回报oracle不再可用，使用蒙特卡洛引入噪声，证书退化为统计估计。
- **手工谓词词汇**：方向、邻接等谓词需人工设计，不能自动发明。缺少谓词发明机制限制了复杂任务的应用。
- **证书空洞**：γ=0.99时回报损失界右侧约2×10⁴，远超实际回报范围（198），无法提供紧贴的保证。需要更细化的界或更小有效折扣。
- **连续任务实验统计弱**：每个任务仅5个教师种子，分辨率扫点稀疏（B=2,3,4,6,8,12），缺少更高分辨率和更多重复。LunarLander上未尝试更大B以验证是否最终收敛到教师。
- **扩展阶段编辑算子有限**：仅为手动定义（添加/重排/剪枝子句），未结合自动谓词发明或基于LLM的生成。编辑候选集有限，可能无法达到全局最优。
- **未与最新可解释方法对比**：没有与VIPER、可微归纳逻辑等方法比较符号学生或教师性能。仅与自身蒸馏变体和教师对比。
- **计算资源未报告**：缺少可复现训练成本的关键信息。

（完）
